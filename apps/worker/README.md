# Alpha Radar worker

The worker reuses the installable `alpha_radar` backend package and exposes it through
`apps/worker/main.py`. Domain task modules will be introduced only in the sprint that owns them.
